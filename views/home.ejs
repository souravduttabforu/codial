<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Codial/<%= title %></h1>

    <div id="posts-list-container">
      <ul>
        <% for(post of posts){ %> 
        <%- include ('_post') %> 
        <% } %>
      </ul>
    </div>
    <section id="user-friens">
      <h4>Friends</h4>
      <% for(u of all_users){ %>
        <p>
          <a href="/users/profile/<%= u.id %>"> <%= u.name %></a>
        </p>
        <% } %>
    </section>
  </body>
</html>
